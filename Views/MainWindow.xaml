<Window x:Class="LARS.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:LARS.ViewModels"
        Title="LARS — Logistics Automation Report System"
        Width="1360" Height="860"
        MinWidth="960" MinHeight="600"
        WindowStartupLocation="CenterScreen"
        Style="{StaticResource LarsWindow}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="56" />
            <RowDefinition Height="*" />
            <RowDefinition Height="36" />
        </Grid.RowDefinitions>

        <!-- ==================== 타이틀 바 ==================== -->
        <Border Grid.Row="0" Background="{StaticResource HeaderGradientBrush}"
                BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,0,1">
            <Grid Margin="20,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <!-- 로고 -->
                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                    <Border Width="34" Height="34" CornerRadius="7"
                            Background="{StaticResource AccentGradientBrush}" Margin="0,0,12,0">
                        <TextBlock Text="L" FontSize="18" FontWeight="Bold"
                                   Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center" />
                    </Border>
                <StackPanel VerticalAlignment="Center">
                        <TextBlock Text="LARS" FontSize="18" FontWeight="Bold"
                                   Foreground="{StaticResource TextPrimaryBrush}" />
                        <TextBlock Text="Logistics Automation Report System" FontSize="10"
                                   Foreground="{StaticResource TextMutedBrush}" Margin="0,-2,0,0" />
                    </StackPanel>
                </StackPanel>

                <!-- 오른쪽 버튼: 매크로 에디터 + 갱신 -->
                <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                    <Button Style="{StaticResource SecondaryButton}" Content="🧩 매크로 에디터"
                            Command="{Binding OpenMacroEditorCommand}" Margin="0,0,8,0" />
                    <Button Style="{StaticResource PrimaryButton}" Content="🔄 전체 갱신"
                            Command="{Binding RefreshAllCommand}" />
                </StackPanel>
            </Grid>
        </Border>

        <!-- ==================== 메인 콘텐츠 ==================== -->
        <TabControl Grid.Row="1" SelectedIndex="{Binding SelectedTabIndex}" Margin="0">

            <!-- ===== AutoReport 탭 (BOM/DailyPlan/PartList/ItemCounter/MultiDocs 통합) ===== -->
            <TabItem Header="📊 AutoReport">
                <TabControl Margin="4,4,4,0">

                    <!-- ===== BOM 하위탭 ===== -->
                    <TabItem Header="📋 BOM">
                <Grid Margin="20,16">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <!-- 액션 바 -->
                    <Border Grid.Row="0" Style="{StaticResource CardPanel}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                <TextBlock Text="BOM (Bill of Materials)" FontSize="16" FontWeight="Bold"
                                           Foreground="{StaticResource TextPrimaryBrush}" />
                                <TextBlock Text="{Binding BomInfoText, StringFormat='  •  {0}'}"
                                           FontSize="12" Foreground="{StaticResource TextSecondaryBrush}"
                                           VerticalAlignment="Center" Margin="8,0,0,0" />
                            </StackPanel>
                            <Button Grid.Column="1" Style="{StaticResource SecondaryButton}"
                                    Content="📡 스캔" Command="{Binding ScanBomFilesCommand}" Margin="0,0,6,0" />
                            <Button Grid.Column="2" Style="{StaticResource SecondaryButton}"
                                    Content="📂 파일 열기" Command="{Binding OpenBomFileCommand}" Margin="0,0,6,0" />
                            <Button Grid.Column="3" Style="{StaticResource PrimaryButton}"
                                    Content="📄 PDF" Command="{Binding ExportBomPdfCommand}" />
                        </Grid>
                    </Border>

                    <!-- 파일 리스트 -->
                    <Expander Grid.Row="1" Header="  스캔된 파일 목록" IsExpanded="False"
                              Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,8">
                        <DataGrid ItemsSource="{Binding BomFiles}" MaxHeight="150">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="파일명" Binding="{Binding FileName}" Width="*" />
                                <DataGridTextColumn Header="상태" Binding="{Binding Status}" Width="80" />
                            </DataGrid.Columns>
                        </DataGrid>
                    </Expander>

                    <!-- 데이터 뷰어 -->
                    <DataGrid Grid.Row="2" ItemsSource="{Binding BomDataTable}"
                              AutoGenerateColumns="True" IsReadOnly="True" />
                </Grid>
                    </TabItem>

                    <!-- ===== DailyPlan 하위탭 ===== -->
                    <TabItem Header="📅 DailyPlan">
                <Grid Margin="20,16">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <Border Grid.Row="0" Style="{StaticResource CardPanel}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                <TextBlock Text="DailyPlan (생산 계획서)" FontSize="16" FontWeight="Bold"
                                           Foreground="{StaticResource TextPrimaryBrush}" />
                                <TextBlock Text="{Binding DpInfoText, StringFormat='  •  {0}'}"
                                           FontSize="12" Foreground="{StaticResource TextSecondaryBrush}"
                                           VerticalAlignment="Center" Margin="8,0,0,0" />
                            </StackPanel>
                            <Button Grid.Column="1" Style="{StaticResource SecondaryButton}"
                                    Content="📡 스캔" Command="{Binding ScanDailyPlanFilesCommand}" Margin="0,0,6,0" />
                            <Button Grid.Column="2" Style="{StaticResource SecondaryButton}"
                                    Content="📂 파일 열기" Command="{Binding OpenDailyPlanFileCommand}" Margin="0,0,6,0" />
                            <Button Grid.Column="3" Style="{StaticResource PrimaryButton}"
                                    Content="📄 PDF" Command="{Binding ExportDpPdfCommand}" />
                        </Grid>
                    </Border>

                    <Expander Grid.Row="1" Header="  스캔된 파일 목록" IsExpanded="False"
                              Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,8">
                        <DataGrid ItemsSource="{Binding DailyPlanFiles}" MaxHeight="150">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="파일명" Binding="{Binding FileName}" Width="*" />
                                <DataGridTextColumn Header="날짜" Binding="{Binding DateValue, StringFormat=yyyy-MM-dd}" Width="100" />
                                <DataGridTextColumn Header="요일" Binding="{Binding WeekdayKorean}" Width="50" />
                                <DataGridTextColumn Header="라인" Binding="{Binding Line}" Width="60" />
                            </DataGrid.Columns>
                        </DataGrid>
                    </Expander>

                    <DataGrid Grid.Row="2" ItemsSource="{Binding DailyPlanDataTable}"
                              AutoGenerateColumns="True" IsReadOnly="True" />
                </Grid>
                    </TabItem>

                    <!-- ===== PartList 하위탭 ===== -->
                    <TabItem Header="📦 PartList">
                <Grid Margin="20,16">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <Border Grid.Row="0" Style="{StaticResource CardPanel}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                <TextBlock Text="PartList (자재 목록)" FontSize="16" FontWeight="Bold"
                                           Foreground="{StaticResource TextPrimaryBrush}" />
                                <TextBlock Text="{Binding PlInfoText, StringFormat='  •  {0}'}"
                                           FontSize="12" Foreground="{StaticResource TextSecondaryBrush}"
                                           VerticalAlignment="Center" Margin="8,0,0,0" />
                            </StackPanel>
                            <Button Grid.Column="1" Style="{StaticResource SecondaryButton}"
                                    Content="📡 스캔" Command="{Binding ScanPartListFilesCommand}" Margin="0,0,6,0" />
                            <Button Grid.Column="2" Style="{StaticResource SecondaryButton}"
                                    Content="📂 파일 열기" Command="{Binding OpenPartListFileCommand}" Margin="0,0,6,0" />
                            <Button Grid.Column="3" Style="{StaticResource SecondaryButton}"
                                    Content="🔧 정규화" Command="{Binding NormalizePartListCommand}"
                                    ToolTip="자재 셀을 [벤더] 파트번호(수량) 표준 형식으로 정규화"
                                    Margin="0,0,6,0" />
                            <Button Grid.Column="4" Style="{StaticResource SecondaryButton}"
                                    Content="🔩 Feeder 필터" Command="{Binding ApplyFeederFilterCommand}"
                                    ToolTip="Feeder 탭에서 선택한 Feeder의 컬럼만 표시"
                                    Margin="0,0,6,0" />
                            <Button Grid.Column="5" Style="{StaticResource SecondaryButton}"
                                    Content="↩ 원본" Command="{Binding ResetToRawCommand}"
                                    ToolTip="정규화/필터 전 원본 데이터로 복원"
                                    Margin="0,0,6,0" />
                            <Button Grid.Column="6" Style="{StaticResource PrimaryButton}"
                                    Content="📄 PDF" Command="{Binding ExportPlPdfCommand}" />
                        </Grid>
                    </Border>

                    <Expander Grid.Row="1" Header="  스캔된 파일 목록" IsExpanded="False"
                              Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,8">
                        <DataGrid ItemsSource="{Binding PartListFiles}" MaxHeight="150">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="파일명" Binding="{Binding FileName}" Width="*" />
                                <DataGridTextColumn Header="날짜" Binding="{Binding DateValue, StringFormat=yyyy-MM-dd}" Width="100" />
                                <DataGridTextColumn Header="라인" Binding="{Binding Line}" Width="60" />
                            </DataGrid.Columns>
                        </DataGrid>
                    </Expander>

                    <DataGrid Grid.Row="2" ItemsSource="{Binding PartListDataTable}"
                              AutoGenerateColumns="True" IsReadOnly="True" />
                </Grid>
                    </TabItem>

                    <!-- ===== MultiDocuments 하위탭 ===== -->
                    <TabItem Header="🔗 교차 매핑">
                <Grid Margin="20,16">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <Border Grid.Row="0" Style="{StaticResource CardPanel}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                <TextBlock Text="BD_MultiDocuments (교차 매핑 자동화)" FontSize="16" FontWeight="Bold"
                                           Foreground="{StaticResource TextPrimaryBrush}" />
                                <TextBlock Text="{Binding MdInfoText, StringFormat='  •  {0}'}"
                                           FontSize="12" Foreground="{StaticResource TextSecondaryBrush}"
                                           VerticalAlignment="Center" Margin="8,0,0,0" />
                            </StackPanel>
                            <Button Grid.Column="1" Style="{StaticResource SecondaryButton}"
                                    Content="📡 결과 스캔" Command="{Binding LoadMultiDocumentsCommand}" Margin="0,0,6,0" />
                            <Button Grid.Column="2" Style="{StaticResource PrimaryButton}"
                                    Content="🚀 일괄 처리 (정규화+PDF)" Command="{Binding ProcessMultiDocumentsCommand}" />
                        </Grid>
                    </Border>

                    <!-- 매핑된 그룹 리스트 -->
                    <DataGrid Grid.Row="1" ItemsSource="{Binding MultiDocuments}"
                              AutoGenerateColumns="False" IsReadOnly="False" Margin="0,4,0,0">
                        <DataGrid.Columns>
                            <DataGridCheckBoxColumn Header="선택" Binding="{Binding IsSelected, UpdateSourceTrigger=PropertyChanged}" Width="50" />
                            <DataGridTextColumn Header="키 (Date_Line)" Binding="{Binding Key}" Width="150" IsReadOnly="True" />
                            <DataGridTextColumn Header="DailyPlan" Binding="{Binding DailyPlanName}" Width="250" IsReadOnly="True" />
                            <DataGridTextColumn Header="PartList" Binding="{Binding PartListName}" Width="250" IsReadOnly="True" />
                            <DataGridCheckBoxColumn Header="매칭 완료" Binding="{Binding HasBoth, Mode=OneWay}" Width="80" IsReadOnly="True" />
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
                    </TabItem>

                    <!-- ===== ItemCounter 하위탭 ===== -->
                    <TabItem Header="🔢 ItemCounter">
                <Grid Margin="20,16">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <Border Grid.Row="0" Style="{StaticResource CardPanel}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                <TextBlock Text="ItemCounter (자재 카운터)" FontSize="16" FontWeight="Bold"
                                           Foreground="{StaticResource TextPrimaryBrush}" />
                                <TextBlock Text="{Binding IcInfoText, StringFormat='  •  {0}'}"
                                           FontSize="12" Foreground="{StaticResource TextSecondaryBrush}"
                                           VerticalAlignment="Center" Margin="8,0,0,0" />
                            </StackPanel>
                            <Button Grid.Column="1" Style="{StaticResource PrimaryButton}"
                                    Content="▶ 집계 실행" Command="{Binding RunItemCounterCommand}" />
                        </Grid>
                    </Border>

                    <DataGrid Grid.Row="1" ItemsSource="{Binding ItemCounterDataTable}"
                              AutoGenerateColumns="True" IsReadOnly="True" Margin="0,4,0,0" />
                </Grid>
                    </TabItem>

                    <!-- ===== Feeder 하위탭 ===== -->
                    <TabItem Header="🔧 Feeder">
                <Grid Margin="20,16">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="320" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <!-- 왼쪽: Feeder 리스트 -->
                    <Grid Grid.Column="0" Margin="0,0,16,0">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>

                        <Border Grid.Row="0" Style="{StaticResource CardPanel}" Padding="12">
                            <StackPanel>
                                <TextBlock Text="Feeder 관리" FontSize="16" FontWeight="Bold"
                                           Foreground="{StaticResource TextPrimaryBrush}" Margin="0,0,0,4" />
                                <TextBlock Text="{Binding FeederInfoText}" FontSize="11"
                                           Foreground="{StaticResource TextMutedBrush}" />
                            </StackPanel>
                        </Border>

                        <!-- 새 Feeder 추가 -->
                        <Border Grid.Row="1" Style="{StaticResource CardPanel}" Padding="12">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <TextBox Text="{Binding NewFeederName, UpdateSourceTrigger=PropertyChanged}"
                                         Margin="0,0,6,0" />
                                <Button Grid.Column="1" Style="{StaticResource PrimaryButton}"
                                        Content="추가" Padding="12,6"
                                        Command="{Binding AddFeederCommand}" />
                            </Grid>
                        </Border>

                        <!-- Feeder 목록 -->
                        <ListBox Grid.Row="2"
                                 ItemsSource="{Binding Feeders}"
                                 SelectedItem="{Binding SelectedFeeder}"
                                 DisplayMemberPath="Name"
                                 Background="{StaticResource SurfaceBrush}"
                                 Foreground="{StaticResource TextPrimaryBrush}"
                                 BorderBrush="{StaticResource BorderBrush}" BorderThickness="1"
                                 Margin="0,4,0,0" />

                        <Button Grid.Row="3" Style="{StaticResource SecondaryButton}"
                                Content="🗑 선택 삭제" Margin="0,8,0,0"
                                Command="{Binding RemoveFeederCommand}" />
                    </Grid>

                    <!-- 오른쪽: Feeder 아이템 -->
                    <Grid Grid.Column="1">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>

                        <Border Grid.Row="0" Style="{StaticResource CardPanel}" Padding="12">
                            <TextBlock FontSize="15" FontWeight="Bold" Foreground="{StaticResource AccentBrush}">
                                <TextBlock.Text>
                                    <MultiBinding StringFormat="Feeder: {0}">
                                        <Binding Path="SelectedFeeder.Name" FallbackValue="(선택 없음)" />
                                    </MultiBinding>
                                </TextBlock.Text>
                            </TextBlock>
                        </Border>

                        <!-- 아이템 추가 -->
                        <Border Grid.Row="1" Style="{StaticResource CardPanel}" Padding="12">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <TextBox Text="{Binding NewFeederItem, UpdateSourceTrigger=PropertyChanged}"
                                         Margin="0,0,6,0" />
                                <Button Grid.Column="1" Style="{StaticResource PrimaryButton}"
                                        Content="아이템 추가" Padding="12,6"
                                        Command="{Binding AddFeederItemCommand}" />
                            </Grid>
                        </Border>

                        <!-- 아이템 목록 -->
                        <ListBox Grid.Row="2"
                                 ItemsSource="{Binding SelectedFeeder.Items}"
                                 Background="{StaticResource SurfaceBrush}"
                                 Foreground="{StaticResource TextPrimaryBrush}"
                                 BorderBrush="{StaticResource BorderBrush}" BorderThickness="1"
                                 Margin="0,4,0,0" />
                    </Grid>
                </Grid>
                    </TabItem>
                </TabControl>
            </TabItem>


            <!-- ===== 설정 최상위 탭 ===== -->
            <TabItem Header="⚙️ 설정">
                <TabControl Margin="4,4,4,0">
                    <!-- 1. 기본/정보 탭 -->
                    <TabItem Header="기본 정보">
                        <Grid Margin="20,16">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="*" />
                            </Grid.RowDefinitions>

                            <Border Grid.Row="0" Style="{StaticResource CardPanel}" Margin="0,0,0,16">
                                <StackPanel>
                                    <TextBlock Text="📁 루트 작업 디렉토리 지정" FontSize="16" FontWeight="Bold"
                                               Foreground="{StaticResource TextPrimaryBrush}" Margin="0,0,0,12" />
                                    <TextBlock Text="모든 파일은 이 폴더 하위의 'LARS_Documents' 내부에서 안전하게 관리됩니다." 
                                               Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,8" FontSize="12"/>
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>
                                        <TextBox Grid.Column="0" Text="{Binding BasePath, Mode=TwoWay}"
                                                 IsReadOnly="True" Margin="0,0,8,0" />
                                        <Button Grid.Column="1" Style="{StaticResource SecondaryButton}"
                                                Content="변경..." Command="{Binding BrowseBasePathCommand}" />
                                    </Grid>
                                </StackPanel>
                            </Border>

                            <Border Grid.Row="1" Style="{StaticResource CardPanel}">
                                <StackPanel>
                                    <TextBlock Text="ℹ️ 앱 정보" FontSize="16" FontWeight="Bold"
                                               Foreground="{StaticResource TextPrimaryBrush}" Margin="0,0,0,12" />
                                    <TextBlock Foreground="{StaticResource TextSecondaryBrush}" FontSize="12">
                                        <Run Text="LARS v1.1" FontWeight="Bold" Foreground="{StaticResource AccentBrush}" />
                                        <LineBreak />
                                        <Run Text="Logistics Automation Report System — C# WPF Edition" />
                                        <LineBreak />
                                        <Run Text=".NET 8.0 | ClosedXML | PdfSharpCore | CommunityToolkit.Mvvm" />
                                        <LineBreak />
                                        <Run Text="모든 라이브러리: MIT 라이선스" Foreground="{StaticResource SuccessBrush}" />
                                    </TextBlock>
                                </StackPanel>
                            </Border>
                        </Grid>
                    </TabItem>

                    <!-- 2. 세부 경로 관리 탭 -->
                    <TabItem Header="경로 관리 (고급)">
                        <ScrollViewer VerticalScrollBarVisibility="Auto">
                            <StackPanel Margin="20,16">
                                <TextBlock Text="경로들을 비워둘 경우, Source(스캔)는 기본 '다운로드' 폴더를, Output(출력/시스템)은 루트 디렉토리 내부 폴더를 기본으로 사용합니다."
                                           Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,16" FontSize="13" FontStyle="Italic" TextWrapping="Wrap"/>

                                <!-- Source 경로 설정 -->
                                <Border Style="{StaticResource CardPanel}" Margin="0,0,0,16">
                                    <StackPanel>
                                        <TextBlock Text="📥 스캔 원본(Source) 폴더 지정" FontSize="16" FontWeight="Bold"
                                                   Foreground="{StaticResource TextPrimaryBrush}" Margin="0,0,0,12" />

                                        <Grid Margin="0,0,0,8">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="80"/>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="60"/>
                                            </Grid.ColumnDefinitions>
                                            <TextBlock Text="BOM" VerticalAlignment="Center" FontWeight="SemiBold"/>
                                            <TextBox Grid.Column="1" Text="{Binding SettingsSourceBom, Mode=TwoWay}" IsReadOnly="True" Margin="0,0,8,0" />
                                            <Button Grid.Column="2" Content="선택" Command="{Binding BrowsePathCommand}" CommandParameter="SourceBOM" Style="{StaticResource SecondaryButton}"/>
                                        </Grid>

                                        <Grid Margin="0,0,0,8">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="80"/>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="60"/>
                                            </Grid.ColumnDefinitions>
                                            <TextBlock Text="DailyPlan" VerticalAlignment="Center" FontWeight="SemiBold"/>
                                            <TextBox Grid.Column="1" Text="{Binding SettingsSourceDailyPlan, Mode=TwoWay}" IsReadOnly="True" Margin="0,0,8,0" />
                                            <Button Grid.Column="2" Content="선택" Command="{Binding BrowsePathCommand}" CommandParameter="SourceDP" Style="{StaticResource SecondaryButton}"/>
                                        </Grid>

                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="80"/>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="60"/>
                                            </Grid.ColumnDefinitions>
                                            <TextBlock Text="PartList" VerticalAlignment="Center" FontWeight="SemiBold"/>
                                            <TextBox Grid.Column="1" Text="{Binding SettingsSourcePartList, Mode=TwoWay}" IsReadOnly="True" Margin="0,0,8,0" />
                                            <Button Grid.Column="2" Content="선택" Command="{Binding BrowsePathCommand}" CommandParameter="SourcePL" Style="{StaticResource SecondaryButton}"/>
                                        </Grid>
                                    </StackPanel>
                                </Border>

                                <!-- Output 경로 설정 -->
                                <Border Style="{StaticResource CardPanel}">
                                    <StackPanel>
                                        <TextBlock Text="📤 출력(Output)/시스템 폴더 지정" FontSize="16" FontWeight="Bold"
                                                   Foreground="{StaticResource TextPrimaryBrush}" Margin="0,0,0,12" />

                                        <Grid Margin="0,0,0,8">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="80"/>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="60"/>
                                            </Grid.ColumnDefinitions>
                                            <TextBlock Text="BOM 출력" VerticalAlignment="Center" FontWeight="SemiBold"/>
                                            <TextBox Grid.Column="1" Text="{Binding SettingsOutputBom, Mode=TwoWay}" IsReadOnly="True" Margin="0,0,8,0" />
                                            <Button Grid.Column="2" Content="선택" Command="{Binding BrowsePathCommand}" CommandParameter="OutBOM" Style="{StaticResource SecondaryButton}"/>
                                        </Grid>

                                        <Grid Margin="0,0,0,8">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="80"/>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="60"/>
                                            </Grid.ColumnDefinitions>
                                            <TextBlock Text="DailyPlan" VerticalAlignment="Center" FontWeight="SemiBold"/>
                                            <TextBox Grid.Column="1" Text="{Binding SettingsOutputDailyPlan, Mode=TwoWay}" IsReadOnly="True" Margin="0,0,8,0" />
                                            <Button Grid.Column="2" Content="선택" Command="{Binding BrowsePathCommand}" CommandParameter="OutDP" Style="{StaticResource SecondaryButton}"/>
                                        </Grid>

                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="80"/>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="60"/>
                                            </Grid.ColumnDefinitions>
                                            <TextBlock Text="PartList" VerticalAlignment="Center" FontWeight="SemiBold"/>
                                            <TextBox Grid.Column="1" Text="{Binding SettingsOutputPartList, Mode=TwoWay}" IsReadOnly="True" Margin="0,0,8,0" />
                                            <Button Grid.Column="2" Content="선택" Command="{Binding BrowsePathCommand}" CommandParameter="OutPL" Style="{StaticResource SecondaryButton}"/>
                                        </Grid>
                                    </StackPanel>
                                </Border>
                            </StackPanel>
                        </ScrollViewer>
                    </TabItem>

                    <!-- 3. Performance 탭 -->
                    <TabItem Header="🚀 Performance (고급)">
                        <Grid Margin="20,16">
                            <Border Style="{StaticResource CardPanel}" VerticalAlignment="Top">
                                <StackPanel>
                                    <TextBlock Text="🚀 다중 코어 처리 (Multi-core Computing)" FontSize="18" FontWeight="Bold"
                                               Foreground="{StaticResource TextPrimaryBrush}" Margin="0,0,0,12" />
                                    <TextBlock Text="실무 환경에서 한 번에 쏟아지는 막대한 양의 문서를 지연 없이 즉각적으로 인식하고 가공할 수 있도록 지원하는 고속 병렬 처리 모듈입니다." 
                                               Foreground="{StaticResource TextSecondaryBrush}" FontSize="13" TextWrapping="Wrap" />
                                </StackPanel>
                            </Border>
                        </Grid>
                    </TabItem>
                </TabControl>
            </TabItem>
        </TabControl>

        <!-- ==================== 상태 바 ==================== -->
        <Border Grid.Row="2" Background="{StaticResource SecondaryDarkBrush}"
                BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,1,0,0">
            <Grid Margin="16,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" Text="{Binding StatusText}"
                           VerticalAlignment="Center" FontSize="12"
                           Foreground="{StaticResource TextSecondaryBrush}" />

                <TextBlock Grid.Column="1" Text="LARS v1.0 | .NET 8 WPF"
                           VerticalAlignment="Center" FontSize="11"
                           Foreground="{StaticResource TextMutedBrush}" />
            </Grid>
        </Border>
    </Grid>
</Window>
