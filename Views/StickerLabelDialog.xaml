<Window x:Class="LARS.Views.StickerLabelDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="🏷️ StickerLabel — 라벨 인쇄 설정"
        Width="680" Height="560"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize"
        MinWidth="520" MinHeight="420">

    <Window.Resources>
        <!-- 간단한 로컬 스타일 (메인 테마 없는 독립 Dialog) -->
        <Style TargetType="Button" x:Key="DlgPrimary">
            <Setter Property="Background"   Value="#1e5fa0" />
            <Setter Property="Foreground"   Value="White" />
            <Setter Property="Padding"      Value="14,6" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Cursor"       Value="Hand" />
            <Setter Property="FontSize"     Value="13" />
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#2a7acc" />
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style TargetType="Button" x:Key="DlgSecondary">
            <Setter Property="Background"   Value="#e5e7eb" />
            <Setter Property="Foreground"   Value="#334155" />
            <Setter Property="Padding"      Value="14,6" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Cursor"       Value="Hand" />
            <Setter Property="FontSize"     Value="13" />
        </Style>
    </Window.Resources>

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- 헤더 -->
        <TextBlock Grid.Row="0" Text="스티커 라벨 설정"
                   FontSize="18" FontWeight="Bold"
                   Foreground="#1e293b" Margin="0,0,0,12" />

        <!-- 설정 패널 -->
        <Border Grid.Row="1"
                Background="#f8fafc"
                BorderBrush="#e2e8f0" BorderThickness="1"
                CornerRadius="6" Padding="14,10" Margin="0,0,0,10">
            <UniformGrid Columns="4" Rows="2">
                <StackPanel Margin="0,0,12,8">
                    <TextBlock Text="라벨 너비 (mm)" FontSize="11" Foreground="#64748b" Margin="0,0,0,2"/>
                    <TextBox x:Name="TxtWidth" Text="70"
                             Padding="6,4" BorderBrush="#cbd5e1" BorderThickness="1" />
                </StackPanel>
                <StackPanel Margin="0,0,12,8">
                    <TextBlock Text="라벨 높이 (mm)" FontSize="11" Foreground="#64748b" Margin="0,0,0,2"/>
                    <TextBox x:Name="TxtHeight" Text="37"
                             Padding="6,4" BorderBrush="#cbd5e1" BorderThickness="1" />
                </StackPanel>
                <StackPanel Margin="0,0,12,8">
                    <TextBlock Text="열 수 (Columns)" FontSize="11" Foreground="#64748b" Margin="0,0,0,2"/>
                    <TextBox x:Name="TxtColumns" Text="2"
                             Padding="6,4" BorderBrush="#cbd5e1" BorderThickness="1" />
                </StackPanel>
                <StackPanel Margin="0,0,0,8">
                    <TextBlock Text="라벨 간격 (mm)" FontSize="11" Foreground="#64748b" Margin="0,0,0,2"/>
                    <TextBox x:Name="TxtGap" Text="3"
                             Padding="6,4" BorderBrush="#cbd5e1" BorderThickness="1" />
                </StackPanel>
                <StackPanel Margin="0,0,12,0">
                    <TextBlock Text="출처" FontSize="11" Foreground="#64748b" Margin="0,0,0,2"/>
                    <ComboBox x:Name="CmbSource"
                              SelectedIndex="0"
                              SelectionChanged="CmbSource_SelectionChanged"
                              Padding="6,4" BorderBrush="#cbd5e1" BorderThickness="1">
                        <ComboBoxItem Content="PartList" />
                        <ComboBoxItem Content="ItemCounter" />
                    </ComboBox>
                </StackPanel>
                <StackPanel Margin="0,0,12,0" VerticalAlignment="Bottom">
                    <TextBlock x:Name="TxtLabelCount"
                               Text="라벨 0개"
                               FontSize="12" FontWeight="SemiBold" Foreground="#1e5fa0" />
                </StackPanel>
            </UniformGrid>
        </Border>

        <!-- 라벨 목록 미리보기 -->
        <DataGrid Grid.Row="2"
                  x:Name="LabelGrid"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  BorderBrush="#e2e8f0" BorderThickness="1"
                  AlternatingRowBackground="#f0f4f8"
                  GridLinesVisibility="Horizontal"
                  Margin="0,0,0,10">
            <DataGrid.Columns>
                <DataGridTextColumn Header="#"          Binding="{Binding No}"         Width="40" />
                <DataGridTextColumn Header="NickName"   Binding="{Binding NickName}"   Width="130" />
                <DataGridTextColumn Header="Vendor"     Binding="{Binding Vendor}"     Width="140" />
                <DataGridTextColumn Header="Part No"    Binding="{Binding PartNumber}" Width="*" />
                <DataGridTextColumn Header="QTY"        Binding="{Binding QTY}"        Width="70" />
            </DataGrid.Columns>
        </DataGrid>

        <!-- 하단 버튼 -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="❌ 닫기"
                    Style="{StaticResource DlgSecondary}"
                    Margin="0,0,10,0"
                    Click="BtnClose_Click" />
            <Button Content="📄 PDF 저장"
                    Style="{StaticResource DlgPrimary}"
                    Click="BtnSave_Click" />
        </StackPanel>
    </Grid>
</Window>
